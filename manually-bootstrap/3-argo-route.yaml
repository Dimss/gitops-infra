apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: argocd-server
spec:
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: passthrough
  port:
    targetPort: http
  to:
    kind: Service
    name: argocd-server
    weight: 100
